server:
  port: 8308
spring:
  application:
    name: nacos-ribbon-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址
service-url:
  user-service: http://nacos-user-service

